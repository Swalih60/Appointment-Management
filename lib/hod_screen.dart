import 'package:flutter/material.dart';

class HodScreen extends StatelessWidget {
  const HodScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Center(child: Text("HOD")),
      ),
      body: const Center(child: Text("HOD")),
    );
  }
}
